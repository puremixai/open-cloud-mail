# Design QA — PureMail

日期：2026-09-06

结果：**本次前端改造范围内的视觉及核心交互验收通过**。此结论限定于下述浏览器环境和场景，不代表全站可访问性认证、真实手机或后端集成验收。

## 设计依据与对照

依据现有代码、三主题样式、`docs/ui-ux-audit-2026-09-05` 与 `docs/idcard-ui-2026-09-06` 的归档截图进行改造。沿用邮件产品的信息结构、经典桌面语言及现有图标资源。

本次是已约定的体验改造，并非像素克隆。手机收件箱基线 `docs/ui-ux-audit-2026-09-05/after-inbox-mobile.png` 与新图 `docs/product-design-2026-09-06/inbox-mobile.png` 在同一次图像比较输入中打开，均为 390×844、收件箱首屏。样例邮件不同，比较针对布局和阅读层级。主要可见差异：移除手机标题栏、菜单栏与底部计数栏；搜索合为一行；首行由约 250px 提前到 158px；星标和验证码减少占位。

手机写信基线 `docs/ui-ux-audit-2026-09-05/after-compose-mobile.png` 与新图 `docs/product-design-2026-09-06/compose-empty-mobile.png` 也在同一次图像输入中比较，均为 390×844。基线有主题文本，新图为空白表单；比较针对标题、字段、正文编辑区、工具栏及发送区。最终图中标题图标完整显示，编辑工具通过更多菜单展开。

## 检查维度

| 维度 | 结论与证据 |
| --- | --- |
| 布局 | 320、390、768、1024、1440 宽度下，页面级 `scrollWidth` 未超过视口；工作区根据实际容器切换，分栏为正文保留至少约 420px 空间。 |
| 字体 | 检查中文、英文、长主题、验证码与附件名。标题、发件人、摘要和时间有明确层级；正文按 16px 基础字号排版；表格邮件允许单独滚动。 |
| 颜色与材质 | 检查浅色、深色、Win95。主题共享尺寸和交互层级；经典主题保留方角与窗口材料；深色 HTML 纸面保留发件方可读颜色。 |
| 图形资源 | 沿用项目图标组件、Iconify 和既有桌面资源；没有引入装饰性占位图片。 |
| 留白与边界 | 检查标题、元信息、正文、附件和回复区域；个人设置一般操作与危险操作分开；手机写信标题与内容均在窗口内。 |
| 文案与状态 | 中文主流程、搜索范围、空白／失败提示、就近输入错误、离线／恢复、本机草稿说明；新增功能提供中英文文案。英文整页未做逐屏视觉验收。 |
| 键盘 | 分栏键盘调整、帮助关闭后 J 导航、列表焦点、隐藏区域 inert；保留已有输入与弹窗的快捷键保护。 |

## 检查中修复的问题

| 问题 | 处理及复核 |
| --- | --- |
| 身份卡默认盖住邮件窗口 | 默认置后，用户主动打开时置前，操作邮件窗口可恢复层级；桌面截图复核。 |
| 导航菜单保留旧固定宽度导致溢出 | 菜单宽度跟随新侧栏，三主题复核。 |
| 重复点击同一封邮件丢失已加载正文 | 相同来源、相同邮件的再次打开保留详情；浏览器连续点击复核。 |
| 分栏关闭后再次启用没有恢复并排布局 | 返回相同来源列表并保留当前邮件；浏览器双向切换复核。 |
| 手机直接打开邮件地址仅显示列表 | 首次测得窄容器后进入完整阅读页；新增自动化回归及浏览器直达复核。 |
| 隐藏帮助弹窗阻挡 J/K/Esc | 只在可见弹窗时阻挡，新增回归测试；打开帮助、关闭、J 跳到下一封实测。 |
| 手机元信息被写信样式意外撑高 | visualViewport 高度规则限制到写信 dialog；手机发件人行约 21px、正文顶部约 279px，重新截图。 |
| 独立阅读页沿用灰色页面底色 | 阅读区域底色优先级明确，经典手机正文恢复白色纸面。 |
| 写信标题负边距造成图标裁切 | 清除旧负边距并调整手机标题；最终图标位于窗口内部，顶部约 17px。 |
| 手机工具栏横向拥挤 | 改为更多菜单展开，保留完整编辑功能。 |
| 深色正文贴近纸面边缘 | 增加纸面内边距，宽邮件缩放按可用正文宽度计算。 |
| 组件展示页缺少独立深色样式 | 显式加载主题变量，修正输入宽度；输入清空后显示错误的深色截图复核。 |

## 功能与工程证据

- 23 个 Vitest 文件、169 项测试通过。新测试覆盖工作区、收件人解析、路由恢复、弹窗关闭后的快捷键和断网／重连。
- 正式 Vite 构建及 PWA 生成通过；`git diff --check` 通过。
- 在正式产物中检索预览 token、模拟 API 错误标记和组件页标题，均未发现。
- 搜索“设计”得到 17 封匹配邮件，当前阅读选择清除，结果范围提示正确。
- 首次失败状态点击重试后恢复邮件列表；空邮箱展示明确说明和刷新入口。
- 经典窗口并排阅读可调整列表宽度；缩窄屏幕保留当前邮件；刷新恢复相同邮件。
- 宽版 HTML 示例保留 980px 原始内容，适应宽度／原始大小按钮可切换。
- 手机输入非法地址出现字段错误；修正为两个地址后成为两个收件人；草稿在重新加载后能恢复；发送通过模拟接口返回提交成功。未发送真实邮件。
- 手机写信容器和可见视口在 390×844 下均为 844px；真实系统键盘仍需设备验收。

## 截图与已知限制

最终截图与预览使用说明见 `docs/product-design-2026-09-06/README.md`。桌面三主题截图为 1440×1000，手机阅读、写信、收件箱为 390×844，另有 320×740、768×1024、1024×900 边界截图。

构建保留既有大分包和 Browserslist 数据过期警告。离线状态以浏览器连接事件为依据，无法代表任意服务端故障。所有可交互预览数据来自本地模拟，真实后端收发、iOS／Android 键盘、屏幕阅读器和性能指标没有在本次浏览器验收中验证。全文检索、云草稿、回收站、CC/BCC 发送未扩展后端能力，详见实现说明。
